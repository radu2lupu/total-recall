# Tricky Inventory Gauntlet

This benchmark intentionally contains subtle allocation bugs.

Goal: make all unit tests pass.

Run tests:

```bash
python3 -m unittest discover -s tests -p "test_*.py"
```
