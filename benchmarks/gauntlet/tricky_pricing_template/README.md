# Tricky Pricing Gauntlet

This benchmark contains subtle pricing bugs around coupon expiry, rounding, and shipping thresholds.

Goal: make all unit tests pass.

Run tests:

```bash
python3 -m unittest discover -s tests -p "test_*.py"
```
